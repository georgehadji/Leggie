# Python
__pycache__/
*.pyc
*.egg-info/
*.egg
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/
eval_results.json
.import_linter_cache/
.mypy_cache/

# Environment
.env
.env.local

# Reasonix / AutoResearch state
.reasonix/

# Graphify output (large generated files)
graphify-out/

# Outputs (generated reports)
Outputs/

# SQLite runtime stores (event store / state store created by a real run).
# leggie.db appeared untracked-and-unignored, so a bulk `git add -A` would
# have committed a 36KB runtime database full of one developer's run history.
*.db
*.db-wal
*.db-shm

# Ad-hoc run artifacts (smoke logs, scratch CLI output dumps)
*.log
analysis_report.docx
analysis_report.md
test_events.jsonl
citation_search.json
parsed.json
openrouter_models.json
e2e_test_results.json

# OS
.DS_Store
Thumbs.db
